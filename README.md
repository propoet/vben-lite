##### Tailwind CSS 安装
```shell
pnpm add -D tailwindcss@3 postcss autoprefixer
pnpm exec tailwindcss init -p
```
- pnpm dlx tailwindcss init -p  没安装，只想生成配置, 建议使用 pnpm exec(项目级标准入口) 命令
- -p生成postcss.config.js


- 读取 *D:\0propoet\project-ready\vben\vue-vben-admin*下的vben 源码,完成以下任务:
- 将过程写成一个完整文档, 包括代码/注释/原理/集成详细步骤/测试步骤 (只需要一个完整的文档, 文档太多太杂乱,不方便查看);
- 如需生成example, 在当前项目example 下新建一个xx文件夹, 在这个文件夹下生成,文档放在./md 下(只需生成一个md文档,包含详细步骤,代码,注释,说明,原理等等必要的说明);
- 在一个基础的 vue3+vite 非monorepo 单体后端管理系统项目(本项目),要实现vben中的 "国际化系统/考虑ui组件库语言包功能(element plus)" 的全部功能;
- 查看本项目结构/依赖/已有代码和功能 (如果现有结构与vben不一致,请按照vben的结构调整,vben-lite只是自己起的临时名称,可以当做其为vben项目);
- 要求生成代码与vben代码完全一致, 不要简化, 不要省略,不要自己发挥,完全按照vben的实现方式;
- 讲透原理;
- 从0开始这个功能, 包括在什么位置创建什么文件,引入什么三方库,三方库作用;
- 有任何依赖的其他模块,一块实现;
- 给出vben代码,一步一步实现,循序渐进,每步可以测试, 最终实现功能; 
- 如果有些配置目前用不到,也要写上,便于后续功能的添加
- 一步步集成, 一步步测试; 给出集成步骤;
- 分析方法的使用场景和原理, 当做注释添加到方法上
- 最后实现完后,检查代码与vben是否完全一致;是否有遗漏的模块, 如果不一致或缺失, 修改到完全一致;

- 此时md 文档已经生成  在./md 下, 只需再次在./example 生成一下示例项目,注意目前可能用不到的配置也要生成
